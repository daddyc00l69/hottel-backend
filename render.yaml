services:
  # Backend API Service
  - type: web
    name: hottel-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: "npm install"
    startCommand: "node simple-server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
